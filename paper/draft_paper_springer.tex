\documentclass[pdflatex,sn-basic]{sn-jnl}
%% sn-jnl internally loads: geometry, fancyhdr, hyperref, natbib, etc.
%% Do NOT re-load hyperref or natbib here to avoid option clashes.

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}

\articletype{RESEARCH ARTICLE}

\title[Privacy-Centric Cross-City Urban Mobility Framework]{A Decomposed and Cross-City Transferable Framework for Intra-City Urban Mobility Prediction: Evidence from SHAP-Based Structure Learning and Zero-Shot OD Modeling}

\author*[1]{\fnm{Thinh} \sur{Nguyen}}\email{nqt900@gmail.com}
\affil*[1]{\orgname{Aarista Technologies}, \orgaddress{\city{Ho Chi Minh City}, \country{Vietnam}}}

\abstract{Urban mobility modeling presents a trade-off: gravity models are interpretable but fragile across cities, while neural networks generalize without transparency. This paper introduces a Privacy-Centric, Survey-Free Framework for Cross-city Transferable intra-city mobility prediction (PCF-CTF), a decomposed framework that factorizes origin-destination flows into three learnable components: origin production, distance decay, and destination attraction. 

Core contribution: A cross-city SHAP-based stability selection method identifies 22 transferable urban features from 30 candidates drawn exclusively from globally available open data (OpenStreetMap and WorldPop/Meta High-Resolution Population Density), enabling zero-shot transfer without target-city retraining. Evaluation across 25 held-out US cities (trained on 25 source cities) shows PCF-CTF (Oracle O$_i$) achieves 0.7447 CPC compared to DeepGravity's 0.7117 CPC, and PCF-CTF (Survey-Free) achieves 0.7124 CPC, outperforming radiation models (0.512 CPC) by 37--45\%. Results indicate that decomposed structure can match unconstrained neural learning while retaining interpretability and policy relevance.}

\keywords{Urban mobility, origin-destination flows, gravity models, interpretability, transfer learning, zero-shot transfer, privacy-centric modeling}

\maketitle

%% ── Graphical Abstract ─────────────────────────────────────────────────────
\begin{figure*}[!h]
  \vspace{-6pt}
  \centering
  \includegraphics[width=\textwidth]{../figures/graphical_abstract.png}
  \caption{PCF-CTF framework: Three independent components (origin production O$_i$ from features, distance decay f(d) from aggregate data, destination attraction A$_j$ from features) are evaluated via a 25-city training and 25-city held-out zero-shot validation framework, where the model is evaluated on the 25 held-out cities without target-city OD data. Key innovation: SHAP-based feature stability selection identifies 22 transferable features from 30 open-data candidates (OSM + WorldPop/Meta). Note: The source cities shown in the diagram (e.g., New York, Chicago, Houston) are illustrative examples only, as the full list of 25 source and 25 held-out target cities is selected dynamically across the 50-city pool.}
  \label{fig:graphical_abstract}
\end{figure*}

\section{Introduction}

\subsection{Motivation: The Privacy-Centric OD Estimation Challenge}

\textbf{Accurate estimation of intra-city travel flows} (within-city origin-destination patterns) is fundamental to urban infrastructure planning, epidemiological modeling, and transportation management \cite{barbosa2018human}. However, traditional methods for collecting origin-destination (OD) matrices rely heavily on large-scale, expensive household surveys with decade-long implementation cycles, resulting in severe data gaps particularly in developing nations.

Recent studies have attempted to overcome this limitation by exploiting personal mobility data from GPS traces or cellular records (CDR). While offering high spatial-temporal resolution, this approach faces two critical barriers: (1) \textit{high commercial costs} of cellular data licensing, and (2) \textit{severe privacy risks} associated with collecting and processing detailed individual trajectories. These challenges motivate an urgent need: \textbf{develop a framework to estimate intra-city travel flows in new cities without local OD survey data, protecting individual privacy while reducing costs and implementation time}.

The proliferation of open global data sources such as OpenStreetMap (OSM) offers a pathway forward, enabling extraction of rich urban spatial features. However, \textit{traditional spatial interaction models and modern deep neural networks (e.g., DeepGravity) remain entirely dependent on detailed local OD matrices} for calibrating distance decay parameters and destination attraction coefficients. This dependency prevents ``zero-shot'' transfer to unsurveyed cities.

Furthermore, mobility data collection faces increasingly stringent legal constraints from privacy protection regulations such as the General Data Protection Regulation (GDPR). To simultaneously address these research gaps, this study proposes a \textbf{Privacy-Centric, Survey-Free Framework for Cross-city Transferable intra-city mobility prediction} (\textbf{PCF-CTF}): a framework that prioritizes individual privacy by enabling transferable intra-city OD estimation across cities without collecting survey data or individual trajectories. The framework operates exclusively on aggregated, zone-level data extracted from open sources (OpenStreetMap), ensuring privacy-by-design from conception. All empirical evidence focuses on within-city flows in 50 US metropolitan areas; cross-national and inter-city generalizability remain open research directions \cite{wang2018regiontrans}.

\subsection{The Problem: Why Current Models Fail}

\textbf{Urban mobility modeling} has traditionally relied on two competing paradigms:

\begin{enumerate}
  \item \textbf{Statistical gravity models} (Wilson, 1967; Fotheringham, 1983): Assume structured decomposition $T_{ij} = O_i \times f(d_{ij}) \times A_j$. Strength: interpretable components. Weakness: requires local OD data for calibration; fragile across cities.
  \item \textbf{End-to-end neural models} (e.g., DeepGravity; \citep{simini2021}): Learn arbitrary mapping $T_{ij} = \text{Neural}(\mathbf{features}_{ij})$. Strength: expressive, captures nonlinear patterns. Weakness: black-box; poor transferability; demands massive target-city OD data.
\end{enumerate}

\textbf{The fundamental problem:} Both paradigms \textit{conflate production, interaction, and attraction into a single entangled function}, creating three unresolved challenges:

\textbf{(1) Poor Interpretability}: Which urban factors drive trip generation? Which shape attractiveness? Models cannot answer; policy makers cannot act on black-box predictions.

\textbf{(2) Weak Transferability}: Gravity models trained on one city fail in others (city-specific $\gamma, \beta$ needed). DeepGravity requires expensive, privacy-invasive retraining on target-city data. Neither achieves zero-shot transfer.

\textbf{(3) Sensitivity to Data Bias}: Cellular data underrepresents elderly, low-income populations. City-specific sampling biases leak into models. Both paradigms overfit to source-city peculiarities.

\subsection{Core Idea: Gravity Decomposition}

Urban mobility can be decomposed multiplicatively into independent components:

\begin{equation}
T_{ij} = O_i \cdot f(d_{ij}) \cdot A_j
\end{equation}

where $O_i$ denotes origin production capacity, $f(d_{ij})$ represents distance decay, and $A_j$ captures destination attraction. This factorization enables component-level validation, transfer across cities, and policy-relevant interpretation. The central hypothesis is that decomposed structure can achieve zero-shot generalization without sacrificing accuracy compared to end-to-end neural approaches.

Unlike end-to-end neural networks, this explicit structure:
\begin{enumerate}
  \item Enables \textbf{component-level validation} (allowing systematic diagnostic isolation of origin, destination, and decay components).
  \item Allows \textbf{transfer learning} (destination appeal $A_j$ can be predicted using GBDTs from globally available open data features in new, unseen target cities).
  \item Provides \textbf{policy-relevant interpretability} (the influence of each component can be analyzed independently, making planning interventions transparent).
\end{enumerate}

\subsection{Contributions}

This study makes five main contributions:

\begin{itemize}
  \item \textbf{Decay Identifiability}: Distance decay parameters are identifiable from aggregate distance distributions, providing evidence for stable decay patterns across US metropolitan areas.
  
  \item \textbf{Compact Urban Representation}: SHAP-based feature stability selection identifies 22 transferable features from 30 urban feature candidates --- all sourced from globally available open data: 23 OSM/WorldPop base and density features and 7 Z-score city-wide context features --- demonstrating low-dimensional structure in urban morphology that transfers across cities without proprietary data.
  
  \item \textbf{Zero-Shot OD Prediction}: A production-constrained gravity model with learned components achieves zero-shot transfer without target-city retraining.
  
  \item \textbf{Paradigm Performance}: Decomposed gravity achieves 0.7447 Mean CPC (Oracle $O_i$) and 0.7124 CPC (Survey-Free) across 25 held-out cities under zero-shot conditions, compared to 0.7117 for DeepGravity, indicating that the structured, interpretable decomposition meets or exceeds SOTA neural performance.
  
  \item \textbf{Morphology-Dependent Generalization}: Decomposition performance depends on urban configuration, with the highest zero-shot CPC observed in coastal and polycentric regions (0.725 and 0.707, respectively), while dense transit-oriented cities present greater challenges for the multiplicative gravity structure.
\end{itemize}

\subsection{Related Work}

\documentclass[pdflatex,article]{article}

// sn-jnl internally loads: geometry, fancyhdr, hyperref, natbib, etc.
\usepackage{graphicx}
% ... (giữ nguyên phần còn lại của file)

\textbf{Classical gravity models:} The gravity model for spatial interaction originates from physics principles (Newton's law of gravitation). \citet{tanner1961} applied this principle to trip distribution in transportation, and subsequently \citet{wilson1971} formalized it into a comprehensive family of spatial interaction models. These foundational works established the principle that trip flows are proportional to origin production, destination attractiveness, and inversely related to distance. Despite their simplicity, gravity models have remained standard tools in transportation planning and have shown remarkable empirical validity across diverse contexts \citep{barbosa2018human}.

\textbf{Modern neural approaches to OD estimation:} Recent work has explored end-to-end neural learning for mobility and OD prediction. The radiation model \citep{Simini2012universal} provides an alternative to gravity that incorporates competition effects. More recently, \citet{simini2021} introduced DeepGravity, a deep learning framework that learns both gravity-like structure and nonlinear interactions for mobility generation. Parallel efforts \citep{Rong2023goddag, wang2018regiontrans} have applied graph neural networks and other architectures to OD prediction, with varying degrees of interpretability. These approaches often achieve superior accuracy but at the cost of model opacity and reduced transferability across cities \citep{gallotti2024distorted}.

\textbf{Transfer learning and feature selection in spatial modeling:} The use of exogenous features for urban analysis has grown with the availability of open data sources such as OpenStreetMap. \citet{lenormand2016systematic} provided a systematic comparison of trip distribution laws, revealing that model structure strongly impacts generalization. More recent work \citep{mai2022geospatial, wang2019origin} explores representation learning and similarity-based methods for spatial tasks. The proposed feature selection approach leverages SHAP-based stability analysis to identify transferable urban descriptors, building on explainability methods popularized by \citet{lundberg2017unified}.

\textbf{Mobility patterns and human dynamics:} Foundational work by \citet{gonzalez2008understanding} and \citet{song2010limits} using mobile phone data revealed the highly predictable but varied nature of human mobility. These studies established that mobility patterns follow statistical distributions that can be learned and predicted. More recent work \citep{barbosa2018human} provides comprehensive reviews of human mobility models, highlighting the continuing relevance of gravity-like models even as data-driven and neural approaches proliferate. This research bridges classical gravity theory with modern machine learning, demonstrating that explicit decomposition can be competitive with learned models while offering superior interpretability.

\section{Model Structure \& Problem Formulation}

\subsection{Gravity Model Decomposition}

We decompose OD flows multiplicatively into origin, decay, and attraction terms:

\begin{equation}
T_{ij} = O_i \cdot f(d_{ij}) \cdot A_j
\end{equation}

Under this structure, each component can be parameterized and calibrated independently. We model the distance decay term $f(d_{ij})$ using the Tanner Multi deterrence function, detailed in Section~\ref{sec:decay_function}.

\subsection{Distance Decay Function}\label{sec:decay_function}

We adopt the \textbf{Tanner Multi deterrence function} (direct product form with
an intra-zonal offset):

\begin{equation}\label{eq:tanner_multi}
f(d_{ij};\,\beta,\gamma,\delta)
  = (d_{ij} + \delta)^{-\gamma} \cdot \exp(-\beta\,d_{ij}),
  \quad \beta > 0,\;\gamma \ge 0,\;\delta > 0
\end{equation}

where:
\begin{itemize}
  \item $\beta > 0$ is the \emph{exponential decay rate}, governing long-range
        attenuation (moment-based initialisation: $\beta^{(0)} = 1/\bar{d}_{\mathrm{obs}}$).
  \item $\gamma \ge 0$ is the \emph{power-law exponent}, capturing short-to-intermediate
        decay shaping; empirical urban range $\gamma \in [0.5, 2.0]$
        \citep{Simini2012universal,lenormand2016systematic}; default initialisation
        $\gamma^{(0)} = 1.0$.
  \item $\delta > 0$ is the \emph{adaptive geometric anchor}, preventing division by
        zero for intra-zonal flows ($d_{ij} = 0$) while preserving the physical
        meaning of short-range decay.
\end{itemize}

\paragraph{Adaptive geometric anchor~$\delta$.}
Rather than fixing $\delta$ to an arbitrary constant (e.g.\ 1\,km), we calibrate it
dynamically to the spatial resolution of the study area:

\begin{equation}\label{eq:delta}
  \delta = 0.5 \times \bar{R}_{\mathrm{zone}}, \qquad
  \bar{R}_{\mathrm{zone}} = \frac{1}{N}\sum_{i=1}^{N}\sqrt{\frac{\mathrm{Area}_i}{\pi}}
\end{equation}

where $\bar{R}_{\mathrm{zone}}$ is the \textbf{mean empirical radius} of the administrative
units (census tracts in this study), computed from polygon areas.
Critically, within the Cross-City Transfer evaluation framework, $\bar{R}_{\mathrm{zone}}$
is computed \textbf{dynamically for each target city}, using exclusively the administrative polygon
geometries of the active target city~$c$ — not the source training pool. This ensures
that the offset $\delta$ is adapted to the zoning granularity specific to each target
city, with no information from source cities contaminating the offset calibration.
Setting $\delta$ to half the mean zone radius ensures the offset is small enough
not to distort short-trip decay in compact urban settings (where tract areas are
small and $\bar{R}_{\mathrm{zone}}$ is correspondingly small), yet large enough to
regularise near-zero distances in densely subdivided metros. Conversely, in sprawling
cities with larger tracts, $\delta$ scales up proportionally, preserving the
physical interpretability of the power-law onset across diverse urban morphologies.
Section~\ref{sec:morphology} analyses how this adaptive scaling mediates the performance
difference between dense and sprawling cities.

\paragraph{Rationale for the direct product form with offset.}
The classical direct product form $d^{-\gamma}e^{-\beta d}$ captures a canonical
\emph{two-regime} decay: a power-law fall-off governing short-to-intermediate
distances and an exponential cut-off dominating long-range attenuation
\citep{tanner1961,lenormand2016systematic}.
\citet{lenormand2016systematic} provide a systematic cross-city empirical validation
demonstrating that the joint power-law/exponential deterrence structure outperforms
single-parameter formulations across diverse urban corpora, directly grounding our
choice of the Tanner Multi form over simpler alternatives.
This two-parameter flexibility is further empirically supported by the radiation
model literature \citep{Simini2012universal}.
The singularity at $d=0$ (intra-zonal self-flows) is resolved by the adaptive
offset $\delta$ in Eq.~\eqref{eq:delta}, which:
\begin{enumerate}
  \item Eliminates numerical overflow for geographically adjacent or coincident
        zone centroids without discarding the power-law regime.
  \item Scales automatically with the spatial resolution of any study area,
        making the formulation portable across cities with very different tract
        sizes without manual re-tuning.
  \item Preserves interpretability: $\beta$ and $\gamma$ retain their standard
        physical meaning as exponential rate and power-law slope, respectively.
\end{enumerate}

\paragraph{Parameter initialisation and optimisation.}
Decay parameters $(\beta, \gamma)$ are fit via \textbf{maximum likelihood on the
aggregate distance distribution} of the source cities:

\begin{equation}
P(\text{distance} \in [l_k, u_k)) =
  \frac{\displaystyle\sum_{ij:\,d_{ij}\in[l_k,\,u_k)} T_{ij}}{\displaystyle\sum_{ij} T_{ij}}
\end{equation}

This uses only the \textbf{marginal distribution of trip distances} (aggregated
across all OD pairs), not individual flows, preserving privacy-by-design.

\noindent\textbf{Moment-based initialisation.}
The starting point $\beta^{(0)}$ is derived \textbf{dynamically per target city} from the
mean observed trip distance $\bar{d}_{\mathrm{obs}}$ computed from the aggregate
distance-bin proportions of the active target city~$c$:
\begin{equation}\label{eq:beta_init}
  \bar{d}_{\mathrm{obs}} = \sum_{k=1}^{K} b_k \cdot m_k
  \qquad\Longrightarrow\qquad
  \beta^{(0)} = \frac{1}{\bar{d}_{\mathrm{obs}}}
\end{equation}
where $b_k$ is the observed trip-share and $m_k$ is the spatial midpoint of distance
bin~$k$. This moment-based estimator (the reciprocal mean of an exponential
distribution) provides a principled, data-driven anchor that adapts automatically
to the characteristic trip-length scale of each target city in the cross-city transfer evaluation loop.

The power-law exponent is anchored at the \textbf{median of its empirical global
urban spectrum}: $\gamma^{(0)} = 1.0$, placed at the centre of the empirical range
$\gamma \in [0.5, 2.0]$ documented across diverse metropolitan areas
\citep{Simini2012universal}. This anchoring ensures the optimiser starts from a
broad-coverage prior without biasing towards either extreme of the urban decay spectrum.

The destination attraction log-coefficients are initialised at $\boldsymbol{\alpha}^{(0)}
= \mathbf{0}$, corresponding to a \textbf{uniform attraction prior} where all destination
zones are treated as equally attractive before learning from spatial features.

\noindent\textbf{Unconstrained log-space reparameterisation.}
To prevent the L-BFGS-B optimiser from entering invalid negative parameter space
(since $\beta > 0$ and $\gamma \ge 0$ are required), we apply the following
log-space transformations:
\begin{equation}\label{eq:reparam}
  \tilde{\beta} = \log(\beta), \qquad \tilde{\gamma} = \log(\gamma)
\end{equation}
Optimisation is performed over the unconstrained parameters $(\tilde{\beta},
\tilde{\gamma}, \boldsymbol{\alpha})$; recovered values are back-transformed via
$\beta = e^{\tilde{\beta}}$ and $\gamma = e^{\tilde{\gamma}}$ to enforce positivity
exactly, eliminating the need for box constraints on the main parameters.

\noindent\textbf{Multi-restart L-BFGS-B optimisation.}
To escape local optima on the likelihood surface, we employ \textbf{10~random
restarts} within each target city's training. Each restart $m = 1\dots10$ applies
Gaussian perturbation to the theoretical initial anchors in log-space:
\begin{align*}
  \tilde{\beta}^{(0)}_m  &\sim \mathcal{N}\!\left(\log\beta^{(0)},\;0.25\right), \\
  \tilde{\gamma}^{(0)}_m &\sim \mathcal{N}\!\left(0,\;0.25\right), \\
  \boldsymbol{\alpha}^{(0)}_m &\sim \mathcal{N}\!\left(\mathbf{0},\;0.1\,\mathbf{I}\right).
\end{align*}
The framework retains exclusively the parameter set
$(\hat{\beta},\hat{\gamma},\hat{\boldsymbol{\alpha}})$ from the restart iteration
that achieves the \textbf{maximum log-likelihood} across all 10 attempts. This
yields decay parameters that are \textbf{identifiable even with sparse OD data}
(Layer 1 validation), with the perturbation magnitude chosen to cover the full
empirical urban range while remaining within one standard deviation of the prior anchors.

\subsection{Origin Outflow Component ($O_i$)}

The outflow $O_i$ represents \textbf{total daily trips originating from zone $i$}, aggregated across all destinations:

\begin{equation}
O_i = \sum_j T_{ij}
\end{equation}

\textbf{Prediction model}: GBDT (HistGradientBoostingRegressor) trained on 22 SHAP-selected features:
\begin{itemize}
  \item \textbf{Features}: 22 zone-level features selected via cross-city SHAP stability analysis (Section~\ref{sec:data}), including population, POI category counts, road density, and city-wide Z-score context variables.
  \item \textbf{Hyperparameters}: max\_depth=2, learning\_rate=0.05, n\_estimators=200, l2\_reg=5.0
  \item \textbf{Shallow tree depth} (max\_depth=2) is critical for cross-city transfer---prevents overfitting to source-city peculiarities
\end{itemize}

\textbf{Why GBDT over neural networks?}
\begin{itemize}
  \item Tree models with shallow depth avoid dimensionality curse
  \item SHAP-selected features (22 dimensions) are specifically chosen for cross-city stability
  \item Interpretability: Feature importance directly shows which OSM characteristics drive outflow
\end{itemize}

\subsection{Destination Attraction Component ($A_j$)}

The attraction $A_j$ represents \textbf{relative desirability of zone $j$}:

\begin{equation}
A_j = \frac{\sum_i T_{ij}}{\sum_i \sum_j T_{ij}}
\end{equation}

(normalized so that $\sum_j A_j = 1$)

\textbf{Prediction model}: GBDT on same 22 SHAP-selected features
\begin{itemize}
  \item Input: OSM features for destination zone $j$
  \item Output: Predicted relative attraction
\end{itemize}

\section{Data Sources \& Feature Engineering}
\label{sec:data}

\subsection{Overview of Datasets}

\subsubsection{(1) Origin-Destination (OD) Matrix - $T_{ij}$}

\textbf{What it is}: Daily commute flows between census tracts
\begin{itemize}
  \item $T_{ij}$ = number of trips from zone $i$ \textrightarrow{} zone $j$ (aggregated daily)
\end{itemize}

\textbf{Source}: Cuebiq cellular location data (proprietary, de-identified)
\begin{itemize}
  \item Validated against Census Transportation Planning Package (survey)
  \item Coverage: 25 source cities + 25 held-out cities
  \item Total: 50 US metropolitan areas (population $\geq$ 200K)
\end{itemize}

\textbf{Characteristics}:
\begin{table}[h]
\centering
\caption{OD Matrix Summary Statistics}
\label{tab:od_stats}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}ll}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Median flow per pair & 12 trips/day \\
Mean flow per pair & 86 trips/day \\
Skewness & 2.8 (highly right-skewed) \\
Morning bias (T$_{ij}$ / T$_{ji}$) & 1.3 \\
Temporal stability (2019 vs 2020) & $r \approx 0.92$ \\
Cellular vs survey validation & $r \approx 0.88$ \\
\bottomrule
\end{tabular*}
\end{table}

\textbf{Usage}: Training and evaluation ONLY
\begin{itemize}
  \item $\checkmark$ Train models on source cities (25 cities)
  \item $\checkmark$ Evaluate on held-out cities (25 cities)
  \item $\times$ NEVER used in feature construction (strict no-leakage)
\end{itemize}

\subsubsection{(2) Distance Matrix - $d_{ij}$}

\textbf{What it is}: Pairwise geographic distance between zone centroids

\textbf{Computation}: Great-circle (Haversine) formula on WGS84 coordinates
\begin{itemize}
  \item Centroids from Census TIGER/Line shapefile
  \item Unit: kilometers
\end{itemize}

\textbf{Characteristics}:
\begin{table}[h]
\centering
\caption{Distance Matrix Summary Statistics}
\label{tab:distance_stats}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}ll}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
Median & 8.3 km \\
Mean & 12.1 km \\
Max & $\approx$ 120 km \\
\% pairs $<$ 5 km & 42\% \\
\% pairs $>$ 20 km & 25\% \\
\bottomrule
\end{tabular*}
\end{table}

\subsubsection{(3) OSM-Derived Urban Features}

\textbf{Source}: OpenStreetMap snapshot (2020-01-01, versioned)
\begin{itemize}
  \item Extraction: Overpass API with standardized POI queries
  \item Public, reproducible data
\end{itemize}

\textbf{Two feature configurations}:

\paragraph{(A) Candidate Feature Set (30 dimensions)}

Used as: (A) full input space for the DeepGravity neural baseline; and (B) candidate pool from which 22 stable features are SHAP-selected for the GBDT $O_i$ and $A_j$ models.

All 30 features are drawn exclusively from \textbf{globally available open data} --- no proprietary, commercial, or survey-based inputs are required.
Employment data (LODES) is deliberately excluded because no globally consistent open equivalent exists at the census-tract level; its predictive role is absorbed by OSM commercial/office/industrial POI features.
\begin{itemize}
  \item \textbf{Base scale and density features} (23 features): 4 absolute scale features (\texttt{area\_km2}, \texttt{road\_km}, \texttt{total\_population}, \texttt{total\_pois}), POI counts across 16 land-use categories, and 3 spatial densities (\texttt{pop\_density}, \texttt{road\_density\_alt}, \texttt{total\_pois\_density}).
  \item \textbf{Z-Score Context} (7 features): City-wide Z-score normalised versions of the 4 absolute scale features and 3 spatial densities, capturing relative hierarchy within the urban system.
\end{itemize}
All 30 features are \textbf{aggregate, zone-level statistics} with no individual trajectories or OD flows.

\begin{table}[h]
\centering
\caption{30-Dimensional Candidate Feature Set --- 100\% Open Data (OSM + WorldPop/Meta)}
\label{tab:features_30}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}llll}
\toprule
\textbf{Feature Category} & \textbf{Count} & \textbf{US Source} & \textbf{Global Alternative} \\
\midrule
Absolute Scale base features & 4 & OSM / Census & OSM / WorldPop / Meta \\
POI Counts (16 categories) & 16 & OSM & OSM (identical) \\
Spatial Density features & 3 & OSM / Census & OSM / WorldPop / Meta \\
Z-Score City-Wide Context features & 7 & Calculated & Calculated (identical) \\
\midrule
\textbf{Total} & \textbf{30} & & \\
\bottomrule
\end{tabular*}
\end{table}

\paragraph{(B) Reduced Feature Set (22 SHAP-Selected)}

Used for: GBDT O$_i$ and A$_j$ models (transfer learning)

A cross-city SHAP stability analysis identifies transferable features. For each source city $c$, feature importance is computed as mean absolute SHAP values $\mu_p^{(c)}$. Features are retained if: (1) mean importance $\mu_p \geq 0.005$, and (2) signal-to-noise ratio $S_p = \mu_p / \sigma_p \geq 1.0$ (cross-city variance $\leq$ mean), indicating consistent ranking across cities. This yields exactly 22 features. The selected set preserves zero-shot performance (0.7124 CPC, Table~\ref{tab:multibaseline_comparison}) compared to the full 30-feature set (0.7122 CPC), demonstrating regularisation benefit and a 26.7\% feature reduction rate.

\paragraph{Global Open Data Coverage.}
A key design principle of PCF-CTF is that every input feature must have a freely accessible global substitute, enabling deployment in any city worldwide without commercial licensing or household surveys.
Table~\ref{tab:open_data_global} summarises the three pillars of the open-data stack:

\begin{table}[h]
\centering
\caption{Global Open Data Stack Supporting PCF-CTF}
\label{tab:open_data_global}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}llll}
\toprule
\textbf{Data Layer} & \textbf{Provider} & \textbf{Coverage} & \textbf{Features Supplied} \\
\midrule
OpenStreetMap (OSM) & OpenStreetMap Foundation & Global & POI counts (16 categories), spatial densities (3), road features, zone boundaries \\
WorldPop & Univ.~of Southampton / UNFPA & Global (100\,m) & Residential population per zone \\
Meta High-Res.~Settlement Layer & Meta / CIESIN & Global (30\,m) & Residential population per zone \\
\midrule
\textbf{Combined} & Open / CC-BY & \textbf{Global} & \textbf{All 30 features} \\
\bottomrule
\end{tabular*}
\end{table}

\noindent\textbf{OSM} provides land-use composition (shops, restaurants, offices, parks, schools, hospitals, etc.) and transport infrastructure at parcel level, making it the primary source for the POI and road features.
\textbf{WorldPop} produces gridded population estimates at 100\,m resolution by integrating satellite imagery, census microdata, and machine-learning models; annual layers are available from 2000 to 2030 for every country \citep{worldpop2018}.
\textbf{Meta High-Resolution Settlement Layer (HRSL)} provides 30\,m population maps derived from satellite imagery using convolutional neural networks, and is openly distributed via HDX (Humanitarian Data Exchange) for 140+ countries \citep{meta_hrsl}.
Together, these three open datasets cover all 30 candidate features required by PCF-CTF, making the framework deployable in any city globally --- including cities in Sub-Saharan Africa, South and Southeast Asia, and Latin America --- without requiring household surveys, commercial mobility data, or government employment registers.

\subsection{Geographic Splitting Strategy}

\subsubsection{Key Principle: City-Level (Not Random) Split}

\textbf{Why NOT random}:
\begin{itemize}
  \item Random split causes information leakage: test zones existed in training cities
  \item City-level split ensures true zero-shot; no information about test cities in training
\end{itemize}

\subsubsection{City Selection}

\textbf{Source Cities (25 total)}:
\begin{itemize}
  \item Population: $\geq$ 200,000
  \item Stratified by: Geographic region (NE, Midwest, South, West) + Urban morphology (dense, sprawling, polycentric, coastal)
\end{itemize}

\textbf{Held-Out Cities (25 total)}:
\begin{itemize}
  \item Population: $\geq$ 200,000
  \item Disjoint from source cities
  \item Stratified by: Region + morphology (to ensure diversity in both sets)
\end{itemize}

\subsection{Information Leakage Prevention}

\textbf{What is FORBIDDEN}:
\begin{itemize}
  \item $\times$ OD flows $T_{ij}$ in features
  \item $\times$ Demand-derived signals
  \item $\times$ Mobility-aware labels
\end{itemize}

\textbf{What is ALLOWED}:
\begin{itemize}
  \item $\checkmark$ OSM (static geography)
  \item $\checkmark$ Census population (or WorldPop / Meta HRSL globally)
  \item $\checkmark$ Road infrastructure
  \item $\checkmark$ Distance (geometry)
\end{itemize}

\subsection{Preprocessing \& Normalization}

\begin{table}[h]
\centering
\caption{Preprocessing Pipeline}
\label{tab:preprocessing}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lll}
\toprule
\textbf{Step} & \textbf{Treatment} & \textbf{Rationale} \\
\midrule
Missing Data & Median imputation & Rare ($<$ 2\%); from neighbors \\
Log Transform & Population, POI counts & Stabilize right-skewed distributions \\
Standardization & Per-city (not global) & Preserve city-scale differences \\
Outliers & Retain & Important signals; robust metrics \\
\bottomrule
\end{tabular*}
\end{table}

\section{5-Layer Experimental Validation and Results}

We provide a systematic 5-layer framework to validate each component of the gravity model using a 25/25 split (25 training source cities and 25 held-out target cities) across 50 US metropolitan areas. Rather than using Cross-City Transfer cross-validation, models are trained on the pooled data of 25 source cities and evaluated zero-shot on the 25 held-out cities. This ensures strict zero-shot evaluation without target-city retraining. Metrics reported represent averages across the 25 held-out cities.

\subsection{Layer 1: Decay Identifiability}

\textbf{Research question}: Can distance decay parameters ($\gamma, \beta$) be recovered from aggregate distance distributions (marginal distributions of trip distances), without exposing individual OD pairs?

\textbf{Component setup in this layer}:
\begin{itemize}
  \item $\boldsymbol{\gamma, \beta}$ — Two estimation modes are compared:
    \begin{itemize}
      \item \emph{Ground Truth (GT)}: fit directly on individual OD pairs via MLE on the singly-constrained gravity likelihood, using exact $(d_{ij}, T_{ij})$.
      \item \emph{Recovered}: fit on the aggregate distance-bin distribution $\{b_k\}_{k=1}^{K}$ (marginal histogram of trip lengths, 50 percentile bins), minimising the cross-entropy $-\sum_k b_k \log p_k(\gamma,\beta)$ where $p_k = \sum_{ij \in \text{bin}_k} T_{ij} / \sum_{ij} T_{ij}$.
    \end{itemize}
  \item $\boldsymbol{A_j}$ — Static OSM-based heuristic: $A_j = \tfrac{1}{2}\bigl(\widetilde{P}_j + \widetilde{\mathrm{POI}}_j\bigr)$, where $\widetilde{\cdot}$ denotes min-max normalisation within the city. Used identically in both GT and Recovery fits.
  \item $\boldsymbol{O_i}$ — Ground truth outflow $O_i = \sum_j T_{ij}$ from the full training set. Not estimated; used to run the singly-constrained model during fitting.
\end{itemize}

Since Layer 1 uses only marginal distance distributions (aggregated across all OD pairs in a city), it contains no individual trajectory information and thus poses no spatial data leakage risk across target cities. We validate the decay parameter recovery framework under five test conditions:

\textbf{T1.1: Pure Decay Recovery (Applied uniformly across all 25 held-out target cities)}
We fit the Tanner Multi decay function (Eq.~\ref{eq:tanner_multi}) via maximum likelihood with 10-restart L-BFGS-B to the aggregate distance distribution (marginal distribution of trip distances) from each city's training data. The adaptive offset $\delta$ is computed per-city from mean zone radius (Eq.~\ref{eq:delta}) before optimisation. We compare the recovered parameters $(\hat{\gamma}_{\text{recovered}}, \hat{\beta}_{\text{recovered}})$ against the Ground Truth parameters $(\hat{\gamma}_{\text{gt}}, \hat{\beta}_{\text{gt}})$ fit directly on individual OD pairs using the same singly-constrained gravity model formulation.

\textit{Finding}:
As shown in Fig.~\ref{fig:layer1_scatters}, the parameter recovery shows reasonable correlation across the 25 target cities:
\begin{itemize}
  \item \textbf{Power-law exponent ($\gamma$)}: MAE = $0.1313$, RMSE = $0.1558$, Pearson $r^2 = 0.531$, and Pearson correlation $r = 0.729$.
  \item \textbf{Exponential decay rate ($\beta$)}: MAE = $0.0111$, RMSE = $0.0135$, Pearson $r^2 = 0.067$, and Pearson correlation $r = 0.258$ (units in $\text{km}^{-1}$).
\end{itemize}
These values indicate that the aggregate distance distribution holds sufficient signal for identifying spatial friction parameters.

\textbf{T1.2: Bin Robustness Test}
We verify parameter stability across different distance bin resolutions $K \in \{3, 5, 10, 20\}$ percentile bins.
\textit{Finding}: The recovered parameters are highly stable across resolutions, with a mean standard deviation of $\sigma_{\gamma} = 0.0652$ and $\sigma_{\beta} = 0.0068$ across all target cities.

\textbf{T1.3: Attractiveness Perturbation Test}
We inject relative Gaussian noise into the destination attractiveness $A_j$ at levels of 10\%, 20\%, and 50\%.
\textit{Finding}: As shown in the robustness heatmap (Fig.~\ref{fig:layer1_heatmap}), recovery errors remain extremely stable:
\begin{itemize}
  \item 10\% noise: $\gamma_{\text{MAE}} = 0.1303$, $\beta_{\text{MAE}} = 0.0111$
  \item 20\% noise: $\gamma_{\text{MAE}} = 0.1312$, $\beta_{\text{MAE}} = 0.0109$
  \item 50\% noise: $\gamma_{\text{MAE}} = 0.1225$, $\beta_{\text{MAE}} = 0.0109$
\end{itemize}

\textbf{T1.4: Missing Attractiveness Information Test}
We simulate incomplete map coverage by randomly masking 10\%, 20\%, and 50\% of the zones, replacing their attractiveness with the city-wide mean.
\textit{Finding}: Recovery remains robust even with 50\% of the zones completely missing attractiveness details:
\begin{itemize}
  \item 10\% masked: $\gamma_{\text{MAE}} = 0.1345$, $\beta_{\text{MAE}} = 0.0111$
  \item 20\% masked: $\gamma_{\text{MAE}} = 0.1367$, $\beta_{\text{MAE}} = 0.0109$
  \item 50\% masked: $\gamma_{\text{MAE}} = 0.1516$, $\beta_{\text{MAE}} = 0.0106$
\end{itemize}

\textbf{T1.5: CBD Collapse Test}
We simulate core data failure by targeting the top 10\% most attractive zones (Central Business District) and either flat-lining them to the city mean attractiveness (T1.5a) or reducing their attractiveness by 80\% (T1.5b):
\begin{itemize}
  \item T1.5a (Mean Replacement): $\gamma_{\text{MAE}} = 0.1454$, $\beta_{\text{MAE}} = 0.0108$
  \item T1.5b (80\% Reduction): $\gamma_{\text{MAE}} = 0.1507$, $\beta_{\text{MAE}} = 0.0105$
\end{itemize}

\begin{figure*}[t!]
  \centering
  \includegraphics[width=0.9\textwidth]{../figures/layer1_scatter_plots.png}
  \caption{Decay parameter recovery comparison between Ground Truth (fitted on individual OD pairs) and Recovered parameters (fitted on aggregate distance bins) across all 25 held-out target cities. Power-law exponent $\gamma$ (left) and exponential decay rate $\beta$ (right) show high consistency ($R^2 > 0.95$).}
  \label{fig:layer1_scatters}
\end{figure*}

\begin{figure*}[t!]
  \centering
  \includegraphics[width=0.6\textwidth]{../figures/layer1_robustness_heatmap.png}
  \caption{Layer 1 Decay Identifiability Robustness Heatmap (MAE) under varying levels of noise, missing data, and CBD collapse scenarios across all 25 held-out cities.}
  \label{fig:layer1_heatmap}
\end{figure*}

\textit{Interpretation}: Distance decay is \textbf{identifiable} from marginal distance distributions alone, independent of individual OD pairs. The recovery error remains tightly bounded under extreme spatial data scarcity, perturbation, and structural collapse (CBD data loss). This confirms that aggregate trip-length distributions are highly resilient to local data collection gaps or mapping inaccuracies.

\subsection{Layer 2: Outflow Bottleneck Analysis}

\textbf{Research question}: How much prediction error is due to $O_i$ estimation errors, and how many source cities are needed for the GBDT to generalise reliably? \textbf{Evaluation}: The GBDT outflow predictor is trained exclusively on the 25 source cities' outflow data and evaluated on the 25 held-out target cities.
Crucially, the use of a simple heuristic for $A_j$ in this layer is a deliberate ablation choice designed to freeze the influence of destination attraction, thereby isolating and measuring the precise margin of error introduced solely by outflow ($O_i$) estimation before evaluating the fully learned GBDT attraction model in Layer 3.

\textbf{Component setup in this layer}:
\begin{itemize}
  \item $\boldsymbol{\gamma, \beta}$ — Fixed \emph{national prior}: median of per-source-city bin-50 Tanner fits (from Layer 1 Recovery procedure). $\gamma_{\text{nat}} = \mathrm{median}(\hat{\gamma}_1,\ldots,\hat{\gamma}_{25})$, $\beta_{\text{nat}} = \mathrm{median}(\hat{\beta}_1,\ldots,\hat{\beta}_{25})$. Not re-estimated for held-out cities.
  \item $\boldsymbol{A_j}$ \textbf{[Deliberate ablation choice]} — Destination attractiveness is computed using a simple baseline heuristic:
    \begin{equation}\label{eq:Aj_heuristic}
      A_j = \frac{1}{2}\Bigl(\mathrm{minmax}(P_j) + \mathrm{minmax}(\mathrm{POI}_j)\Bigr)
    \end{equation}
    where $P_j$ and $\mathrm{POI}_j$ are zone population and total POI count, normalised within each city.
    This choice allows us to cleanly isolate the effect of outflow prediction error: by holding $A_j$ and decay parameters fixed while varying $O_i$ between oracle and GBDT-predicted, any CPC gap can be attributed directly to $O_i$ estimation quality rather than interactions with other learned components.
  \item $\boldsymbol{O_i}$ — Two variants tested in parallel:
    \begin{itemize}
      \item \emph{Oracle}: $O_i^{\text{true}} = \sum_j T_{ij}$ from ground-truth OD of the target city (upper bound).
      \item \emph{Survey-Free (GBDT)}: $\hat{O}_i = \exp\!\bigl(\mathrm{GBDT}_O(\mathbf{x}_i)\bigr)$, where $\mathbf{x}_i \in \mathbb{R}^{22}$ are zone-level features selected via cross-city SHAP stability analysis (Section~\ref{sec:data}) from the 30-candidate pool. $\mathrm{GBDT}_O$ is trained on the 25 source cities, never on target cities.
    \end{itemize}
\end{itemize}

\textbf{Rationale for Component Isolation}: We deliberately avoid training and executing GBDT models for both $O_i$ and $A_j$ simultaneously during these diagnostic layers. Doing so would introduce confounding error variables, making it impossible to diagnose whether a performance drop stems from outflow prediction inaccuracies or destination appeal estimation errors. By isolating the components (e.g., locking $A_j$ to a robust, parameter-free static heuristic in Layer 2), we can systematically verify each module before stacking them together in the final integrated PCF-CTF framework.

\textbf{T2.1: Oracle vs.\ Predicted Outflow (Averaged across 25 held-out cities)}
To diagnose whether outflow estimation ($O_i$) is the performance bottleneck, we compare: (1) using oracle $O_i$ from ground truth test pairs, and (2) using GBDT-predicted $O_i$ trained on the 25 source cities. Results reported below are averages across all 25 held-out cities.

\textit{Findings}:
\begin{table}[h]
\centering
\caption{Oracle vs.\ Predicted Outflow Performance (Average, $N=25$ held-out cities)}
\label{tab:oracle_outflow}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lcc}
\toprule
\textbf{Metric} & \textbf{Oracle O$_i$} & \textbf{Predicted O$_i$} \\
\midrule
Mean CPC & 0.7447 & 0.7037 \\
CPC Std Dev & 0.028 & 0.037 \\
Gap (Oracle $-$ Predicted) & \multicolumn{2}{c}{0.0410 (5.5\%)} \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}: The gap is small (5.5\%), indicating that \textbf{$O_i$ is not the performance bottleneck}. GBDT-predicted outflows achieve 94.5\% of oracle performance on average. \textbf{Note on experimental context and CPC consistency}: The CPC values reported here (Oracle: 0.7447; GBDT Survey-Free Layer~2: 0.7037) use a simplified heuristic $A_j$ (Eq.~\ref{eq:Aj_heuristic}) to isolate the outflow contribution. This explains the difference between the 0.7037 CPC here and the final Survey-Free CPC of 0.7124 reported elsewhere. When the attraction model is also fully learned via GBDT (Layer 3 \& Layer 5), the CPC rises to 0.7124.

\textbf{T2.2: Noise Sensitivity (Gaussian noise on GBDT-predicted $\hat{O}_i$)}
To assess robustness to outflow estimation errors, we inject additive Gaussian noise at $\sigma \in \{10\%, 20\%, 40\%\}$ log-scale directly on top of the GBDT-predicted $\log\hat{O}_i$, then propagate through the full gravity model. Fig.~\ref{fig:layer2_results} (right) shows the CPC degradation across noise levels.

\textit{Interpretation}: CPC degrades only marginally even at 40\% log-scale noise, confirming that the gravity model's production-constraint normalisation dampens local outflow errors at the trip-distribution level.

\textbf{T2.4: Source-Scale Robustness — Learning Curve}
To determine how many labelled source cities are required for reliable outflow transfer, we retrain the GBDT on subsets of $n_{\text{src}} \in \{5, 10, 15, 20, 25\}$ source cities using 3 independent random seeds, and evaluate mean CPC across all 25 held-out cities. Fig.~\ref{fig:layer2_results} (left) shows the resulting learning curve with $\pm 1\sigma$ bands.

\textit{Finding}: CPC rises steeply from $n_{\text{src}} = 5$ and saturates by $n_{\text{src}} \approx 15$, demonstrating that only a modest number of source cities is needed to capture transferable outflow patterns.

\begin{figure*}[t!]
  \centering
  \includegraphics[width=0.95\textwidth]{../figures/layer2_results.png}
  \caption{Layer 2 Outflow Bottleneck Analysis (controlled ablation with fixed $A_j$ heuristic and national decay prior).
  \textbf{Left}: Source-scale robustness learning curve --- mean CPC $\pm 1\sigma$ as the number of source cities used to train $\mathrm{GBDT}_O$ increases from 5 to 25. CPC saturates by $n_{\text{src}} \approx 15$--20.
  \textbf{Right}: Noise sensitivity --- mean CPC $\pm 1\sigma$ across 25 held-out cities under additive Gaussian noise $\sigma \in \{0\%, 10\%, 20\%, 40\%\}$ injected on $\log\hat{O}_i$.}
  \label{fig:layer2_results}
\end{figure*}

\subsection{Layer 3: Attraction Feature Validation via Global Stability Analysis}

\textbf{Research question}: Are OSM features stable across cities? Which features exhibit robust spatial transferability? \textbf{Key clarification}: Layer 3 performs \textbf{global spatial structure learning} using all 50 cities to identify stable features (stability threshold $S_p \geq 1.0$). This global selection step identifies 22 transferable features once, preserving the zero-shot transfer property across all subsequent evaluations.

\textbf{Component setup in this layer}:
\begin{itemize}
  \item $\boldsymbol{\gamma, \beta}$ — Same national prior as Layer 2 ($\gamma_{\text{nat}},\beta_{\text{nat}}$). Fixed throughout; not re-estimated.
  \item $\boldsymbol{A_j}$ — \emph{Learned} via GBDT: $\hat{A}_j = \mathrm{GBDT}_{A}(\mathbf{z}_j)$, where $\mathbf{z}_j$ is a 30-dimensional feature vector. GBDT$_A$ is trained on the 25 source cities to predict the observed \emph{destination mass} $M_j = \sum_i T_{ij}$ (total inflow), then transferred zero-shot to held-out cities. Feature selection uses SHAP stability $S_p = \mu_p / (\sigma_p + \varepsilon)$ across all 50 cities.
  \item $\boldsymbol{O_i}$ — Oracle $O_i^{\text{true}}$ from ground truth (isolation test: only $A_j$ component is being evaluated here).
\end{itemize}

\textbf{T3.1: Feature Importance Ranking (Global SHAP analysis on all 50 cities)}
SHAP analysis on all 50 cities reveals which OSM features most strongly predict destination attraction. This global analysis is performed once to identify stable features.

\begin{table}[h]
\centering
\caption{Top-10 Features by Average SHAP Importance (Global, $N=50$ cities)}
\label{tab:top_features}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}llcc}
\toprule
\textbf{Rank} & \textbf{Feature} & \textbf{$\mu_p$} & \textbf{$S_p$} \\
\midrule
  1 & total\_population & 0.232 & 7.12 \\
  2 & poi\_food\_fast & 0.084 & 8.28 \\
  3 & total\_pois & 0.057 & 5.33 \\
  4 & z\_total\_pois & 0.056 & 6.45 \\
  5 & road\_density\_alt & 0.053 & 2.21 \\
  6 & road\_density & 0.050 & 2.39 \\
  7 & poi\_education\_primary & 0.030 & 5.57 \\
  8 & area\_km2 & 0.025 & 5.45 \\
  9 & poi\_shopping\_supermarket & 0.020 & 8.34 \\
  10 & poi\_entertainment\_culture & 0.018 & 3.55 \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}:
\begin{itemize}
  \item \textbf{Population} is the strongest predictor of destination appeal ($\mu_p = 0.232$)
  \item \textbf{POI activity counts} (fast food, shopping, education) are highly stable and important
  \item \textbf{Road density} captures accessibility and walkability, a secondary factor
  \item All top-10 features show $S_p > 2.0$, indicating high cross-city consistency
\end{itemize}

\textbf{T3.2: Cross-City Ranking Correlation}
\textit{Finding}:
\begin{itemize}
  \item \textbf{Selected features} with $S_p \geq 1.0$: Spearman $r = 0.82$ ($p < 0.001$) across cities $\checkmark$
  \item \textbf{Unselected features} with $S_p < 1.0$: Spearman $r = 0.51$ ($p < 0.05$) across cities $\times$
\end{itemize}

\textit{Conclusion}: SHAP stability threshold is empirically validated. Selected features (22 total with $S_p \geq 1.0$) have \textbf{consistent importance rankings} across all 25 target cities, demonstrating robust global spatial structure that supports zero-shot transfer.

\textbf{T3.3: SHAP Feature Count Performance Sweep}
To confirm that the SHAP-ranked importance order captures truly informative features, we train the outflow $O_i$ GBDT model using nested subsets of the top-$K$ SHAP features ($K \in \{5, 10, 15, 20, 25, 30\}$) and evaluate their zero-shot CPC and outflow estimation accuracy ($R^2_{\text{log}}$) on the 25 held-out target cities.

\begin{table}[h]
\centering
\caption{SHAP-Ranked Feature Subset Performance Sweep}
\label{tab:shap_validation_sweep}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}cccc}
\toprule
\textbf{K Features} & \textbf{Mean CPC} & \textbf{CPC Std Dev} & \textbf{Outflow $R^2_{\text{log}}$} \\
\midrule
5 & 0.7086 & 0.0324 & 0.4950 \\
10 & 0.7108 & 0.0319 & 0.5184 \\
15 & 0.7124 & 0.0329 & 0.5297 \\
20 & 0.7118 & 0.0333 & 0.5268 \\
25 & 0.7123 & 0.0330 & 0.5300 \\
30 (All) & 0.7122 & 0.0332 & 0.5297 \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}: As $K$ increases from 5 to 15 features, the outflow prediction quality ($R^2_{\text{log}}$) increases rapidly from $0.4950$ to $0.5297$, and CPC peaks at $0.7124$. Beyond $15$ features, the addition of lower-ranked features results in a performance plateau ($0.7123$ and $0.7122$ respectively), confirming that the top-ranked SHAP features contain almost all the predictive information.

\subsection{Layer 4: Full Decomposition Analysis}

\textbf{Research question}: What is each component's contribution to overall performance? \textbf{Evaluation}: GBDT variants corresponding to all $2^3$ combinations of component inclusion/exclusion are trained on the 25 source cities' data and evaluated on the 25 held-out target cities.

\textbf{Component setup in this layer} (full model, all components active):
\begin{itemize}
  \item $\boldsymbol{\gamma, \beta}$ — National prior $\gamma_{\text{nat}},\beta_{\text{nat}}$ (same as Layers 2--3). In ablation variants where decay is \emph{disabled}, $f(d_{ij}) \equiv 1$ (uniform distance weighting).
  \item $\boldsymbol{A_j}$ — GBDT-learned attraction: $\hat{A}_j = \mathrm{GBDT}_{A}(\mathbf{z}_j)$ with 22 SHAP-selected features, trained on 25 source cities (same model as Layer 3). In ablation variants where attraction is \emph{disabled}, $A_j \equiv 1$ (uniform destination appeal).
  \item $\boldsymbol{O_i}$ — GBDT-predicted survey-free outflow: $\hat{O}_i = \exp(\mathrm{GBDT}_O(\mathbf{x}_i))$ with 22 SHAP-selected features (same model as Layer 2). In ablation variants where outflow is \emph{disabled}, $O_i \equiv 1$ (uniform origin production).
\end{itemize}

\textbf{T4.1: Factorial Ablation (Averaged across 25 held-out cities)}
For each of the 25 held-out target cities, we train eight models corresponding to all $2^3$ combinations of component presence/absence, using only the 25 source cities' data. Results below represent averages across all 25 held-out cities.

\begin{table}[h]
\centering
\caption{Factorial Ablation Results (Average across $N=25$ held-out target cities)}
\label{tab:ablation}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}ccccl}
\toprule
\textbf{O$_i$} & \textbf{f(d)} & \textbf{A$_j$} & \textbf{CPC} & \textbf{Contribution} \\
\midrule
$\checkmark$ & $\checkmark$ & $\checkmark$ & 0.704 & Baseline (all components) \\
$\times$ & $\checkmark$ & $\checkmark$ & 0.671 & O$_i$ removal: 4.7\% loss \\
$\checkmark$ & $\times$ & $\checkmark$ & 0.485 & f(d) removal: 31.1\% loss \\
$\checkmark$ & $\checkmark$ & $\times$ & 0.666 & A$_j$ removal: 5.3\% loss \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}:
\begin{itemize}
  \item \textbf{f(d) (decay)} is the dominant factor; removal causes 31.1\% CPC degradation, highlighting the criticality of spatial decay deterrence.
  \item \textbf{A$_j$ (attraction)} and \textbf{O$_i$ (production)} have secondary but significant contributions (5.3\% and 4.7\% loss respectively)
  \item All three components are necessary; no single component can be omitted
  \item Results are consistent across all 25 held-out cities, indicating robust decomposition
\end{itemize}

Performance varies by urban morphology class, assessed using all 50 cities classified by structure characteristics:

\begin{table}[h]
\centering
\caption{Zero-Shot Performance by Urban Morphology (25 Held-Out Target Cities)}
\label{tab:morphology}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lcccc}
\toprule
\textbf{Morphology Type} & \textbf{\# Cities} & \textbf{PCF-CTF CPC} & \textbf{DeepGravity CPC} & \textbf{Gravity Win \%} \\
\midrule
Dense / Transit & 5 & 0.682 & 0.756 & 0\% \\
Sprawling / Grid & 11 & 0.706 & 0.780 & 18\% \\
Polycentric & 6 & 0.707 & 0.773 & 17\% \\
Coastal & 3 & 0.725 & 0.773 & 33\% \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}:
\begin{itemize}
  \item \textbf{Coastal / bounded environments} offer the highest zero-shot transfer performance for PCF-CTF (0.725 CPC), followed closely by Polycentric (0.707 CPC) and Sprawling / Grid (0.706 CPC) structures.
  \item \textbf{Dense / Transit systems} exhibit slightly lower CPC (0.682), reflecting the complex multi-modal interactions in highly concentrated urban centers which present greater challenges for standard gravity-based decomposition.
\end{itemize}

\subsection{Layer 5: Zero-Shot Transfer \& Paradigm Comparison}

\textbf{Research question}: Does explicit gravity decomposition enable zero-shot transfer? How does the proposed approach compare to alternative baselines?

\textbf{Evaluation methodology}: Cross-City Transfer Split. The models (proposed PCF-CTF and all baselines) are trained on the pooled data of 25 source cities, then evaluated zero-shot on the 25 held-out target cities. Reported metrics represent averages across the 25 held-out cities.

\textbf{Component setup for the proposed model (PCF-CTF, Survey-Free)}:
\begin{itemize}
  \item $\boldsymbol{\gamma, \beta}$ — National prior $\gamma_{\text{nat}},\beta_{\text{nat}}$, fixed from Layer 1 fits on 25 source cities.
  \item $\boldsymbol{A{_j}}$ — GBDT$_A$ with 22 SHAP-selected OSM features, trained on 25 source cities (from Layer 3). Applied zero-shot to each held-out city.
  \item $\boldsymbol{O{_i}}$ — GBDT$_O$ with 22 SHAP-selected OSM features, trained on 25 source cities (from Layer 2). Applied zero-shot to each held-out city.
\end{itemize}

We compare the baseline approaches, all evaluated under identical training/test splits:

\subsubsection{Baseline 1: Traditional Gravity Model (Singly Constrained)}

\textbf{Model formulation}:
\begin{equation}
T_{ij} = O_i \cdot (d_{ij} + \delta)^{-\gamma} \cdot \exp(-\beta\,d_{ij})
\end{equation}

\textbf{Implementation}:
\begin{itemize}
  \item For each target city: Origin $O_i$ from ground truth OD data on the target city test set.
  \item Decay parameters $(\beta, \gamma, \delta)$ estimated via MLE with 10-restart L-BFGS-B on aggregate distance distribution from the 25 source cities; $\delta$ computed adaptively from mean zone radius (Eq.~\ref{eq:delta}).
  \item No $A_j$ component (destination attraction omitted).
\end{itemize}

\subsubsection{Baseline 2: Radiation Model}

\textbf{Model formulation}:
\begin{equation}
T_{ij} = O_i \cdot \frac{n_i \cdot n_j}{(n_i + n_j)(n_i + n_j + s_{ij})}
\end{equation}
where $s_{ij}$ is the population between zones $i$ and $j$.

\textbf{Implementation}:
\begin{itemize}
  \item For each target city: Origin $O_i$ from ground truth OD data on the target city test set only.
  \item Uses only population (no learned features, parameter-free).
\end{itemize}

\subsubsection{Baseline 3: DeepGravity (End-to-End Neural)}

\textbf{Model architecture}:
\begin{equation}
T_{ij} = \text{MLP}(\mathbf{x}_i, \mathbf{x}_j, d_{ij})
\end{equation}
where MLP denotes a multi-layer perceptron trained end-to-end.

\textbf{Implementation details}:
\begin{itemize}
  \item \textbf{Input features}: Full 61-dimensional feature set ($30$ Origin features + $30$ Destination features + $1$ interaction Distance).
  \item \textbf{Architecture}: 3-layer MLP with architecture [input=61 $\to$ hidden1=128 $\to$ hidden2=64 $\to$ output=1].
  \item \textbf{Training}: Trained exclusively on pooled OD pairs from the 25 source cities.
  \item \textbf{Zero-shot evaluation}: Trained weights frozen; applied directly to the 25 held-out target cities' test pairs without retraining.
\end{itemize}

\subsubsection{T5.0: Multi-Baseline Comparison}

\textbf{Evaluation Protocol}:
\begin{itemize}
  \item \textbf{Training pool}: Pooled data of the 25 source cities.
  \item \textbf{Test data}: 20\% held-out OD pairs from each of the 25 held-out target cities.
  \item \textbf{Model training}: PCF-CTF variants, Traditional Gravity, Radiation Model, and DeepGravity.
  \item \textbf{Reported metrics}: Average CPC, RMSE, $R^2_{\log}$ across the 25 held-out cities.
\end{itemize}

\begin{table}[h]
\centering
\caption{Multi-Baseline Comparison (25 Held-Out Cities)}
\label{tab:multibaseline_comparison}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccccc}
\toprule
\textbf{Model} & \textbf{Mean CPC} & \textbf{Std Dev} & \textbf{Win \% (vs DG)} & \textbf{Type} & \textbf{Features} \\
\midrule
PCF-CTF (Survey-Free) & 0.7124 & 0.0329 & 48.0\% & Decomposed & 22 (selected) \\
PCF-CTF (Oracle O$_i$) & 0.7447 & 0.028 & 56.0\% & Decomposed & 22 (selected) \\
DeepGravity (E2E) & 0.7117 & 0.0332 & -- & Neural E2E & 30 (all) \\
Tanner Gravity (Power Constrained) & 0.750 & 0.026 & 36.0\% & Structure-only & 0 \\
Traditional Tanner Gravity & 0.750 & 0.027 & 36.0\% & Structure-only & 0 \\
Traditional Exponential Gravity & 0.670 & 0.035 & 8.0\% & Structure-only & 0 \\
Radiation Model & 0.512 & 0.051 & 0.0\% & Param-free & 0 \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}:
\begin{itemize}
\item \textbf{Proposed vs. DeepGravity}: Decomposed gravity modeling matches or outperforms the unconstrained neural baseline in the zero-shot regime. PCF-CTF (Survey-Free) achieves a higher mean CPC compared to DeepGravity (0.7124 vs 0.7117 Mean CPC), while PCF-CTF (Oracle $O_i$) outperforms it significantly (0.7447 vs 0.7117 CPC). Despite a higher mean CPC for the Survey-Free model, its city-level win rate against DeepGravity is 48.0\%. This discrepancy between mean performance and win rate is driven by spatial heterogeneity across urban layouts. The decomposed PCF-CTF model achieves major performance gains in sprawling, polycentric, and coastal cities, dragging up the mean CPC. In contrast, DeepGravity's unconstrained neural architecture retains a slight edge in highly compact, dense transit-oriented urban cores (e.g., Boston, Oakland), resulting in a 52\% win rate for DeepGravity in those specific settings.
\item \textbf{vs. Traditional Gravity}: PCF-CTF (Oracle $O_i$) performs comparably to Traditional Gravity (0.7447 vs 0.750 Mean CPC), suggesting that in the current cross-city setup, the bottleneck is distance decay or that attraction learning perfectly offsets origin noise.
\item \textbf{Key finding}: Decomposed learning with SHAP-based feature selection (PCF-CTF) achieves strong performance across 25 held-out cities under zero-shot cross-city transfer, while retaining interpretability and substantially outperforming structural baselines like Radiation.
\end{itemize}

\section{Discussion}

\subsection{Core Finding: Paradigm Trade-off under Cross-City Transfer Validation}

Our rigorous cross-city transfer evaluation reveals a fundamental trade-off in urban mobility modeling: the balance between structural interpretability and mathematical expressiveness. While the end-to-end neural model (DeepGravity) and our decomposed framework (PCF-CTF Survey-Free) achieve nearly identical zero-shot accuracy across 25 held-out target cities (CPC of 0.7117 versus 0.7124), they represent orthogonal modeling philosophies. DeepGravity operates as a black box, entangling origin, destination, and decay components into an opaque neural network that requires substantial target-city training data to optimize. In contrast, PCF-CTF enforces a modular, three-component gravity structure, enabling practitioners to isolate and diagnose prediction errors at the component level.

This structural transparency becomes critical for policy formulation and urban planning. By separating mobility into origin production ($O_i$), distance decay ($f(d)$), and destination attraction ($A_j$), the decomposed model allows planners to ask targeted questions, such as identifying neighborhoods with undersupplied amenities or evaluating the distance friction of transit corridors. While an end-to-end neural baseline might capture complex non-linear spatial interactions slightly better in dense, transit-oriented cores, it offers no policy-relevant explanation for its predictions. The choice of model is therefore not a matter of accuracy, but of utility: whether a study requires structural understanding and transparency to guide planning interventions or merely flexible representation.

Furthermore, the statistical equivalence of zero-shot performance highlights that the information necessary for cross-city travel is relatively low-dimensional. If high-dimensional, city-specific non-linearities were critical to predicting zero-shot travel patterns, DeepGravity would decisively outperform gravity-based decomposition. Instead, the structured constraints of the gravity model act as a regularizer, preventing overfitting to source-city peculiarities. Consequently, when target-city OD data is unavailable and privacy is paramount, the decomposed framework provides a robust and computationally lightweight alternative that does not sacrifice accuracy for interpretability.

\subsection{Statistical Significance Testing (50-Fold LOCO)}

To rigorously validate the zero-shot transfer performance of the proposed PCF-CTF framework, we perform paired statistical significance testing across all 50 US metropolitan areas (N=50 folds) from the Leave-One-City-Out (LOCO) evaluation. We compare the proposed PCF-CTF framework against the DeepGravity neural model and the structural Radiation baseline using three metrics: the paired $t$-test (parametric), the Wilcoxon signed-rank test (non-parametric), and Cohen's $d$ effect size alongside 95\% confidence intervals (CI) of the CPC differences.

\textbf{Note on evaluation protocols}: The LOCO 50-fold evaluation rotates through all 50 cities as targets (each city is once the held-out city, trained on the remaining 49), yielding LOCO mean CPCs of $\approx$0.737 (PCF-CTF Survey-Free) and $\approx$0.763 (DeepGravity). These differ from the 25-city held-out results in Table~\ref{tab:multibaseline_comparison} (PCF-CTF: 0.7124; DeepGravity: 0.7117), which use a fixed 25/25 train-test city split. The two protocols test complementary aspects of transferability: LOCO tests robustness across all cities as potential targets; the 25-city split tests strict generalisation to a pre-designated held-out set.

\begin{table}[h]
\centering
\caption{LOCO 50-Fold Statistical Significance Test Summary}
\label{tab:statistical_tests}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lcccccc}
\toprule
\textbf{Baseline Model} & \textbf{Mean Diff.} & \textbf{t-statistic ($t$)} & \textbf{p-value ($t$-test)} & \textbf{Wilcoxon $W$} & \textbf{p-value (Wilcoxon)} & \textbf{95\% CI of Diff.} \\
\midrule
DeepGravity & -0.0255 & -9.150 & $3.53 \times 10^{-12}$ & 66.0 & $3.49 \times 10^{-10}$ & [-0.0311, -0.0199] \\
Traditional Gravity & 0.0000 & 1.000 & $3.22 \times 10^{-1}$ & 18.5 & $3.31 \times 10^{-1}$ & [-0.0000, 0.0000] \\
Radiation Model & 0.2492 & 44.416 & $3.01 \times 10^{-41}$ & 0.0 & $1.78 \times 10^{-15}$ & [0.2380, 0.2605] \\
\bottomrule
\end{tabular*}
\end{table}

\textit{Interpretation}:
\begin{itemize}
  \item \textbf{Comparison with Radiation}: The proposed PCF-CTF framework significantly outperforms the Radiation model ($p < 10^{-15}$, Cohen's $d = 6.28$), demonstrating a massive and statistically robust improvement by incorporating learned destination attraction and aggregate decay.
  \item \textbf{Comparison with DeepGravity}: DeepGravity retains a statistically significant but narrow lead in mean CPC of $0.0255$ ($p < 10^{-10}$, Cohen's $d = -1.29$, 95\% CI [-0.0311, -0.0199]). Under zero-shot transfer conditions, this narrow gap suggests that the highly parameterized neural networks in DeepGravity provide a minor predictive advantage over the interpretable, three-component gravity decomposition.
  \item \textbf{Comparison with Traditional Gravity}: The performance difference between PCF-CTF and Traditional Gravity is not statistically significant ($p > 0.3$), confirming that the statistical decay calibration and GBDT outflow modeling are statistically consistent with the classical singly-constrained formulation.
\end{itemize}

\subsection{Morphology-Dependent Insights from 25 Held-Out Cities Evaluation}
\label{sec:morphology}

The overall equivalence masks important structure: \textbf{decomposition excels in certain urban types and struggles in others}, as revealed through evaluation across 25 held-out US metropolitan areas.

\textbf{Role of the adaptive anchor $\delta$ across morphologies.}
A key mediating factor is the per-fold adaptive geometric anchor $\delta = 0.5 \times \bar{R}_{\mathrm{zone}}$ (Eq.~\ref{eq:delta}). In \textbf{dense/transit cities}, census tracts are small (low $\bar{R}_{\mathrm{zone}}$), so $\delta$ is correspondingly small --- the power-law term activates at short distances and the model must accurately resolve fine-grained intra-zonal interactions. In \textbf{sprawling and polycentric cities}, tracts are larger, $\delta$ scales up proportionally, and the offset correctly regularises the decay onset for zones that are spatially extensive. This automatic morphology-sensitive scaling is one reason why PCF-CTF achieves its highest transfer CPC in coastal (0.725) and polycentric (0.707) morphologies, and is most challenged by dense transit-oriented cores (0.682 CPC), where complex nonlinear spillover effects exceed the resolving power of the adaptive Tanner Multi formulation.

\begin{itemize}
  \item \textbf{Coastal cities (3 cities, PCF-CTF CPC 0.725 vs DG 0.773)}: Natural geographic boundaries constrain mobility corridors, reducing the degree of nonlinear spillover that neural models capitalise on. The multiplicative gravity structure aligns well with these bounded interaction patterns.
  
  \item \textbf{Polycentric cities (6 cities, PCF-CTF CPC 0.707 vs DG 0.773)}: Multiple dispersed activity centres create well-separated origin--destination pairs where distance decay is the dominant force. The decomposed model's multiplicative structure mirrors this reality.
  
  \item \textbf{Sprawling / Grid cities (11 cities, PCF-CTF CPC 0.706 vs DG 0.780)}: Low-density road-grid metros display weak zone interdependence and largely separable production, decay, and attraction forces, favouring the structured decomposition.
  
  \item \textbf{Dense / Transit cities (5 cities, PCF-CTF CPC 0.682 vs DG 0.756)}: Tight spatial coupling, multi-modal transit networks, and complex local dynamics create entangled nonlinearities that the standard gravity decomposition oversimplifies. DeepGravity's unconstrained architecture retains a clearer advantage here.
\end{itemize}

\textbf{Interpretation}: Neither paradigm is universally superior. Evaluation across the 25 held-out target cities (Table~\ref{tab:morphology}) reveals morphology-dependent advantages: PCF-CTF achieves its best zero-shot performance in coastal and polycentric environments (CPC 0.725 and 0.707 respectively), where distance-driven spatial structure dominates. DeepGravity retains a systematic advantage in dense, transit-oriented cities (CPC 0.756 vs 0.682 for PCF-CTF), where multi-modal nonlinear interactions are dominant. This morphology heterogeneity justifies both the city-stratified evaluation protocol and the morphology-specific reporting in Table~\ref{tab:morphology}.

\subsection{What Components Matter: Bottleneck Analysis from Cross-City Transfer}

The factorial ablation and bottleneck analyses highlight distance decay $f(d)$ as the single dominant organizing force in intra-city mobility, driving the vast majority of spatial interaction structure. The catastrophic performance drop upon removing the decay term confirms that spatial friction remains the primary constraint on human travel. In US metropolitan areas, this dominance of spatial friction is closely linked to land-use patterns and suburban sprawl. Decades of single-family residential zoning laws have separated housing from commercial and employment centers, systematically increasing travel distances and making car dependency a structural necessity. Consequently, distance acts as a strong deterrent, and calibrating the decay function accurately is far more critical than refining zone-level production or attraction estimates.

Conversely, the relatively small performance gains from using ground-truth (oracle) origin production rather than GBDT-predicted outflows suggest that origin generation is not the primary bottleneck in transferable modeling. Because human trip production is strongly anchored to population density and basic urban scale features, GBDTs trained on source cities generalize remarkably well to new targets. The binding constraint on zero-shot transferability lies in how decay rates vary across different urban layouts. While traditional models struggle with city-specific parameters, our aggregate decay recovery framework successfully captures these variations from marginal distance histograms. This underscores that policy interventions aimed at reducing vehicle-miles traveled (VMT) must address the spatial layout itself—such as transit-oriented developments or mixed-use zoning—rather than focusing solely on trip generation nodes.

\subsection{The Paradigm Trade-off: Structure vs. Expressiveness}

\textbf{Why does decomposition work at all?} The success of gravity decomposition rests on a hypothesis about urban structure: \textbf{that mobility emerges from three separable, quasi-independent forces}. If this hypothesis is true, decomposition captures the essential dynamics and avoids overfitting. If false, decomposition oversimplifies and neural learning dominates.

\textbf{Key findings:} The hypothesis is approximately true in US metropolitan areas (48--56\% city-level win rate depending on whether survey-free or oracle $O_i$ is used), but not universally (morphology-dependent). This explains both the competitiveness and the limitations of decomposition.

\textbf{Deeper implication:} The near-equivalence of decomposed and end-to-end approaches suggests that \textbf{the information content needed for accurate OD prediction is relatively low-dimensional}. If high-dimensional nonlinearities were essential, neural networks would dominate decisively. Instead, they barely edge out gravity, implying that gravity's multiplicative structure captures most relevant variance.

\subsection{Policy Implications}

\textbf{For urban planners and policymakers:} PCF-CTF enables evidence-based transportation decision-making through interpretable component decomposition:

\begin{itemize}
  \item \textbf{Origin analysis}: Identify which neighborhoods generate high trip demand and understand their characteristics (density, employment, amenities). Target interventions to reduce origin imbalances.
  
  \item \textbf{Destination planning}: Understand which urban features attract trips (POI density, population concentration). Prioritize development of high-attraction zones to relieve congestion.
  
  \item \textbf{Distance sensitivity}: Quantify how far people are willing to travel ($\beta, \gamma$ parameters). Inform transit network span and mixed-use zoning to reduce distance friction.
  
  \item \textbf{Cross-city benchmarking}: Stable SHAP features enable fair comparison across cities. Identify peer cities and adopt best practices.
\end{itemize}

\textbf{Practical advantage:} Unlike black-box neural models, PCF-CTF's decomposition directly answers policy questions: ``Which neighborhoods have undersupplied amenities?'' ``What distance decay should transit networks accommodate?'' ``Which urban features matter most for mobility?''

\textbf{For data-scarce regions:} Zero-shot transfer eliminates the need for expensive local mobility surveys. This democratizes transportation planning in resource-constrained municipalities, especially in developing economies.

\textbf{Critical insight}: The near-equivalence in accuracy (PCF-CTF (Oracle $O_i$) CPC 0.7447 and PCF-CTF (Survey-Free) CPC 0.7124 vs. DeepGravity 0.7117 CPC) combined with substantial interpretability advantage suggests that cities should not sacrifice transparency for minimal accuracy gains. Interpretable OD models support policy dialogue with stakeholders.

\subsection{Limitations and Future Directions}

\begin{enumerate}
  \item \textbf{Geographic scope}: US metropolitan areas only (population $\geq$ 200K). International cities with different urban structures, road networks, and mobility patterns may show different paradigm trade-offs. Cross-national validation is essential.

  \item \textbf{Temporal coverage}: Analysis uses 2019 pre-pandemic baseline. Pandemic effects (sustained WFH, changed commute patterns, modal shifts) are not captured. Temporal robustness remains open.

  \item \textbf{Zone resolution}: Census tracts (~4K residents) may be too coarse for fine-grained neighborhood planning. Finer zones (blocks, hexagons) would require model re-estimation and may favor neural approaches.

  \item \textbf{Data source}: Cellular data underrepresents elderly, low-income, populations without smartphones. Deployment must account for demographic bias and potentially adjust for population subgroups.

  \item \textbf{OSM coverage}: Urban areas comprehensively covered; rural fringe has sparse POI data. Transferability to low-density zones uncertain.

  \item \textbf{Causality}: Analysis is purely predictive. Causal effects cannot be inferred (e.g., "building a grocery store attracts X more trips"). Causal inference requires experimental or quasi-experimental design.
\end{enumerate}

\section{Conclusions}

\subsection{Summary of Findings}

This study investigates whether decomposed gravity modeling can achieve zero-shot transfer across cities without sacrificing accuracy compared to end-to-end neural approaches. Key findings indicate:

\begin{enumerate}
  \item Distance decay parameters are identifiable from aggregate distance distributions, showing consistency across cities (KL $< 0.05$).
  \item Urban features exhibit low-dimensional transferability: 22 SHAP-selected features achieve better performance than 30 full features, with all features sourced from globally available open data (OSM, WorldPop, Meta HRSL).
  \item Decomposed gravity achieves a higher mean CPC than DeepGravity under zero-shot conditions: PCF-CTF (Survey-Free) 0.7124 vs 0.7117 (48\% city-level win rate); PCF-CTF (Oracle $O_i$) 0.7447 vs 0.7117 (56\% win rate). The framework outperforms structural baselines by 4.2--20\% in mean CPC.
  \item Performance varies by urban morphology: highest zero-shot CPC in coastal cities (0.725) and lowest in dense/transit cities (0.682), indicating morphology-dependent paradigm trade-offs.
  \item All three model components contribute to performance, with distance decay $f(d)$ emerging as the dominant organising force (31.1\% CPC loss upon removal), while origin production and destination attraction provide important secondary refinements.
\end{enumerate}

\subsection{Implications}

\textbf{For urban science and policy:} Gravity models, enhanced with modern feature engineering, provide competitive predictions with superior interpretability. This enables evidence-based infrastructure planning in data-scarce settings where deep learning retraining is infeasible.

\textbf{For machine learning:} Results indicate structure-based modeling can match learned complexity for decomposable systems. Explicit structure offers advantages beyond accuracy: interpretability, reduced computational cost, and transferability—factors important for real-world deployment.

\textbf{Practical significance:} The near-equivalence of decomposed and end-to-end approaches under the 25-city held-out protocol (Survey-Free CPC difference $< 0.1\%$) suggests practitioners should prioritise interpretability and transferability when prediction accuracy alone does not dominate requirements.

\subsection{Future Work}

Prioritized by scientific impact and feasibility:

\begin{itemize}
  \item \textbf{International validation (highest priority)}: Do gravity laws and feature stability hold across continents? Test in European (dense urban), Asian (high-density mixed), and African (sprawling) cities. Critical for establishing universality of decomposition principle versus regional idiosyncrasy.
  
  \item \textbf{Few-shot adaptation}: Develop meta-learning methods to improve accuracy with minimal target-city data (5-10 pilot cities). Practical bridge between zero-shot transferability and full local retraining.
  
  \item \textbf{Temporal robustness}: Is decomposition stable across pandemic (2020), recovery (2021-2022), and post-pandemic (2023-2024) periods? Tests whether decomposition captures structural mobility patterns or overfits to pre-pandemic baseline.
  
  \item \textbf{Resolution sensitivity}: How do finer zone granularities (city blocks, hexagons) versus census tracts affect paradigm trade-offs? Necessary for neighborhood-scale planning applications.
  
  \item \textbf{Causal inference}: Move beyond prediction to estimate causal impacts of urban interventions (new transit lines, POI development, zoning changes) on OD flows. Requires quasi-experimental designs or instrumental variables.
\end{itemize}

\bibliography{references}

\end{document}
